pipeline {
    agent any

    stages {
        stage('Sonar Analysis') {
            steps {
                echo 'Testing..'
                sh 'cd webapp && sudo docker run  sudo docker run  --rm -e SONAR_HOST_URL="http://54.167.46.63:9000" -e SONAR_LOGIN="sqp_cd16fcc61eba41e2841b27c728458c42760560a7"  -v ".:/usr/src" sonarsource/sonar-scanner-cli -Dsonar.projectKey=lms


            }
        }


    }
}
