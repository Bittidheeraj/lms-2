pipeline {
    agent any

    stages {
        stage('Sonar Analysis') {
            steps {
                echo 'Testing..'
                sh 'cd webapp && sudo docker run  --rm -e SONAR_HOST_URL="http://3.83.248.48:9000" -e SONAR_LOGIN="sqp_3e22862e947cf11d556790f2bcc7dc0b800a50be"  -v ".:/usr/src" sonarsource/sonar-scanner-cli -Dsonar.projectKey=lms'
            }
            }    }

}
